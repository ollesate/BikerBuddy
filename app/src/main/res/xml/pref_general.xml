<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <EditTextPreference
        android:key="pref1"
        android:title="Signal a maneuver in advance (seconds)"
        android:defaultValue="10"
        android:selectAllOnFocus="true"
        android:numeric="integer"
        android:singleLine="true"
        android:maxLines="1" />

    <ListPreference
        android:key="pref2"
        android:title="Signal color"
        android:entries="@array/pref_signal_color"
        android:entryValues="@array/pref_signal_color"
        android:defaultValue="180"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />



</PreferenceScreen>
